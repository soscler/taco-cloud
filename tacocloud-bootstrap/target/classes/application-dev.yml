# Development configuration properties

spring:

  thymeleaf:
    cache: false

  jpa:
    show-sql: true

  datasource:
    url: "jdbc:h2:~/test;IFEXISTS=FALSE;"

logging:
  level:
    root: Warn
    org:
      springframework.security: DEBUG
    tacos: INFO
    web: INFO
